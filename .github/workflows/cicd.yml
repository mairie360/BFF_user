name: BFF User CICD

on:
  push:
  pull_request:
  workflow_dispatch:

permissions:
  contents: write
  packages: write

jobs:
  CICD:
    uses: mairie360/CICD/.github/workflows/BFFs-cicd.yml@v1.14.0
    with:
      package_name: bff-user
      cicd_version: v1.14.0
      node_version: "22"
    secrets: inherit
